---
const { variant, text, icon = null } = Astro.props;
const variants = {
  primary: "bg-brand text-white hover:bg-indigo-500",
  secondary: "text-gray-900 font-semibold py-2",
};
const classes = `px-3 text-sm rounded-md inline-flex items-center gap-1 ${
  variants[variant] || ""
}`;
---

<button class={classes}>{text}</button>
















<!-- ---
const { variant, text, icon = null } = Astro.props;

const variants = {
  primary: "bg-brand text-white hover:bg-indigo-500",
  secondary: "text-gray-900 font-semibold py-2",
};
const classes = `px-3 text-sm rounded-md inline-flex items-center gap-1 ${
  variants[variant] || ""
}`;
---

<button class={classes.trim()}>
  {icon}
  <span>{text}</span>
</button> -->
